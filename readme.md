# Bazel minimum example

## How to build and start the docker container

```shell
docker compose run --build bazel-example
```

## How to execute your first Bazel target

```shell
bazelisk run //:generate_sample_files
```
